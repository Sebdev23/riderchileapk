name: ridechile_app
description: "RideChile MTB - Plataforma comunitaria de ciclismo de montana"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  http: ^1.2.2
  provider: ^6.1.2
  google_sign_in: ^6.2.2
  sign_in_with_apple: ^6.1.4
  shared_preferences: ^2.3.4
  geolocator: ^13.0.2
  supabase_flutter: ^2.8.4
  flutter_secure_storage: ^9.2.4
  file_picker: ^8.1.7
  xml: ^6.5.0
  share_plus: ^10.1.4
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  screenshot: ^3.0.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
